//https://introcs.cs.princeton.edu/java/12types/


/*1.  sets a, b, and t equal to the original value of a.
int t = a;
b = t;
a = b; */

//4. (!(a < b) && !(a > b)) this equals to (a == b)

//6. Why does 10/3 give 3 and not 3.33333333? = because integer division

//7. What do each of the following print?
System.out.println(2 + "bc"); //prints: 2bc
System.out.println(2 + 3 + "bc"); //prints: 5bc
System.out.println((2+3) + "bc"); //prints: 5bc
System.out.println("bc" + (2+3)); //prints: bc5
System.out.println("bc" + 2 + 3); //prints: bc23 

//8. Math.sqrt(); is used to find the square root of a number

//16. double force = G * mass1 * mass2 / r * r; 
//corrected answer = double force = G * mass1 * mass2 / (r * r); 

//18. takes two integers x and y and prints the Euclidean distance from the point (x, y) to the origin (0, 0).
import java.util.Scanner;
public class Distance {
    public static void main(String[] args){
        Scanner scan = new Scanner(System.in);
        System.out.print("enter x: ");
        int x = scan.nextInt();
        System.out.print("enter y: ");
        int y = scan.nextInt();

        int product = x*x + y*y;
        int distance = (int) java.lang.Math.sqrt(product);
        System.out.print(distance);
    }
}    

//19. Write a program Divisibility.java that reads in two command line inputs and prints true if both are divisible by 7, and false otherwise.
import java.util.Scanner;

public class Divisibility {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        System.out.print("enter first number: ");
        int num1 = s.nextInt();
        System.out.print("enter second number: ");
        int num2 = s.nextInt();
        
        if(num1%7==0 && num2%7==0){
            System.out.print("true");
        }
        else{
            System.out.print("false");
        }
    }
}

//20. prints the sum of two random integers between 1 and 6 (such as you might get when rolling dice).
import java.util.Random;
public class DiceRoller {
    public static void main(String[] args) {
        Random random = new Random();

        int dice1 = random.nextInt(7);
        int dice2 = random.nextInt(7);
        int sum = dice1 + dice2;

        System.out.println("dice 1: " + dice1);
        System.out.println("dcie 2: " + dice2);
        System.out.println("sum of the two dices: " + sum);
    }
}

//21. Write a program SumOfSines.java that takes a double command-line argument t (in degrees) and prints the value of sin(2t) + sin(3t).
import java.util.Scanner;
public class SumOfSines {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        System.out.print("enter num (degrees): ");
        double t = s.nextDouble();
        
        double sum = Math.sin(2 * t) + Math.sin(3 * t);
        System.out.println("sin(2t) + sin(3t) is "+sum);
    }
}

//22. Body mass index. The body mass index (BMI) is the ratio of the weight of a person (in kilograms) to the square of the height (in meters). Write a program BMI.java that takes two command-line arguments, weight and height, and prints the BMI.
import java.util.Scanner;

public class bmi_calculator {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        System.out.print("enter weight in kgs: ");
        double weight = scan.nextDouble();
        System.out.print("enter height in cm: ");
        double height = scan.nextDouble();
        
        double bmi = (weight / (height*height))*10000;
        System.out.print("BMI: "+ (int) bmi);
    }
}  
        
//23. takes two int values m and d from the command line and prints true if day d of month m is between March 20 (m = 3, d =20) and June 20 (m = 6, d = 20), false otherwise.
import java.util.Scanner;

public class SpringSeason {
    public static void main(String[] args) {
       Scanner scan = new Scanner(System.in);
       System.out.print("Enter month as a number: ");
       int m = scan.nextInt();
       System.out.print("Enter day: ");
       int d = scan.nextInt();
       
       boolean check;
       
       if ((m == 3 && d >= 20 && d <= 31) || (m == 4 && d >= 1 && d <= 30) || (m == 5 && d >= 1 && d <= 31) || (m == 6 && d >= 1 && d <= 20)) {
           check = true;
       } else {
           check = false;
       }
       
       System.out.println("Is it spring season? " + check);
    }
}

//Creative exercises

//25. takes two double command-line arguments t and v and prints the wind chill.
import java.util.Scanner;
public class WindChill {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in); 
        System.out.print("enter temperature (F): ");
        int t = scan.nextInt();
        System.out.print("enter wind speed (miles/hour): ");
        int v = scan.nextInt();
        
        double w = (35.74 + 0.6215*t + (0.4275*t - 35.75) * Math.pow(v, 0.16));
        
        System.out.print("the wind chill is "+w);
    }
}

//26. Polar coordinates. Write a program CartesianToPolar.java that converts from Cartesian to polar coordinates. 
import java.util.Scanner;
public class SumOfSines {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        System.out.print("enter num (x): ");
        int x = s.nextInt();
        System.out.print("enter num (y): ");
        int y = s.nextInt();
        
        double coordinates = Math.atan2(y, x);
        System.out.println("polar coordinates "+coordinates);
    }
}


//29. takes a date as input and prints the day of the week that date falls on.
import java.util.Scanner;
public class DayOfWeek {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in); 
        System.out.print("enter month: ");
        int m = scan.nextInt();
        System.out.print("enter day: ");
        int d = scan.nextInt();
        System.out.print("enter year: ");
        int y = scan.nextInt();
        
        int y0 = y - (14 - m) / 12;
        int x = y0 + y0 / 4 - y0 / 100 + y0 / 400;
        int m0 = m + 12 * ((14 - m) / 12) - 2;
        int d0 = (d + x + 31 * m0 / 12) % 7;

        System.out.println("day of the week: "+ d0);
    
       
    }
}

//30. Uniform random numbers. Write a program Stats5.java that prints five uniform random values between 0 and 1, their average value, and their minimum and maximum value. Use Math.random(), Math.min(), and Math.max().
import java.util.Scanner;
public class SumOfSines {
    public static void main(String[] args) {
        int n = 5;
        double x1 = Math.random();
        double x2 = Math.random();
        double x3 = Math.random();
        double x4 = Math.random();
        double x5 = Math.random();

        System.out.println(x1);
        System.out.println(x2);
        System.out.println(x3);
        System.out.println(x4);
        System.out.println(x5);

        double min     = Math.min(x1, Math.min(x2, Math.min(x3, Math.min(x4, x5))));
        double max     = Math.max(x1, Math.max(x2, Math.max(x3, Math.max(x4, x5))));
        double average = (x1 + x2 + x3 + x4 + x5) / n;

        // print stats
        System.out.println("Average = " + average);
        System.out.println("Min     = " + min);
        System.out.println("Max     = " + max);
    }
}


//34. takes three int values from the command line and prints them in ascending order.
import java.util.Scanner;
public class ThreeSort {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in); 
        System.out.print("enter first number: ");
        int x = scan.nextInt();
        System.out.print("enter second number: ");
        int y = scan.nextInt();
        System.out.print("enter third number: ");
        int z = scan.nextInt();
        
        int min = Math.min(Math.min(x, y), z);
        int max = Math.max(Math.max(x, y), z);
        int middle;
        if(min == x && max == y || min == y || max == x){
            middle = z;
        }
        else if(min == z && max == y || min == y || max == z){
            middle = x;
        }
        else{
            middle = y;
        }
        
        System.out.print("the numbers in ascending order: "+min+middle+max);
       
    }
}

